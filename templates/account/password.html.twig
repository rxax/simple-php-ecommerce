{% extends 'base.html.twig' %}

{% block title %}Change Password - Webshop{% endblock %}
{% block body %}
    <h2>Change Password</h2>
    {{form(form)}}
    {% for message in app.flashes('notice') %}
        <div class="alert alert-danger">
            {{ message }}
        </div>
    {% endfor %}
    <a title="Back to member area" href="{{ path('account') }}">Back to my account</a>
{% endblock %}
